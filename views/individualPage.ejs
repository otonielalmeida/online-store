<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="../css/individualPage.css">
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HomePage</title>
</head>
<body>
    
    <nav>
        <div class="nav-items-left">
            <h2>Home</h2>
            <h2>About</h2>
        </div>
        <div class="nav-items-right">
            <% if (!user) { %> 
                <h2><a href="user/login">Log in</a></h2>
            <% } %>

            <% if (user) { %>
                <h2>hello, <%= user %></h2>
            <% } %>

            <% if (!user) { %>
                <h2><a href="user/register">Register</a></h2>
            <% } %>

            <% if (user) { %> 
                <h2><a href="/logout">Logout</a></h2>
            <% } %>
        </div>
    </nav>
    <div class="product">         
        <div class="product_header">
                <%= individualProduct.name %>
        </div>
        <div class="product_price_img">
            <div id="productImg">    
                <img src="../img/<%= individualProduct.image %>">
    
                <!-- <button id="buy-btn" >Buy</button> -->
            
            </div>
            <div id="product_info">
                <h3>R$ <%= individualProduct.price %></h3>
            </div>
        </div>
        
    </div>
</body>
</html>
